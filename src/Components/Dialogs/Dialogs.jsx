import React from "react";
import f from './Dialogs.module.css'

const Dialogs = (props) => {
    return (
        <div className={`${f.item} ${f.content}`}>
            Dialogs
        </div>
    );
}
export default Dialogs;